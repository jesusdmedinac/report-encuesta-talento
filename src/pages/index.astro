x---
import ReportLayout from '../layouts/ReportLayout.astro';
const links = [
  { href: '/gemini', title: 'Reporte General', provider: 'Google', description: 'Versión generada con Google Gemini' },
  { href: '/openai', title: 'Reporte General', provider: 'OpenAI', description: 'Narrativa generada con OpenAI' },
  { href: '/respuestas', title: 'Listado de respuestas', description: 'Explora el índice administrativo con búsqueda y scroll infinito' },
  { href: '/analisis', title: 'Análisis SEP25', description: 'Análisis técnico del estudio SEP25' }
];
---
<ReportLayout title="Banco Guayaquil · Reportes y Análisis" current="home">
  <main class="home-shell">
    <section class="home-hero">
      <h1>Panel del Reporte de Estudio de Talento</h1>
      <p>Selecciona la vista que necesitas para navegar el diagnóstico del Banco Guayaquil. Todos los datos están alineados al estudio “Escala de Madurez Digital SEP25”.</p>
      <div class="cta-grid">
        {links.map(link => (
          <a class="cta-card" href={link.href}>
            <h2>{link.title} <span class="badge provider">{link.provider || ''}</span></h2>
            <p>{link.description}</p>
            <span>Ir al {link.title.split(' ')[0]} →</span>
          </a>
        ))}
      </div>
    </section>
  </main>

  <style>
    .badge { display:inline-block; margin-left:.5rem; padding:.15rem .5rem; border-radius:999px; font-size:1rem; background:#e5e7eb; color:#0f172a; }
    .badge.provider { background:#e0f2fe; color:#075985; }

    .home-shell {
      max-width: 1120px;
      margin: 0 auto;
      padding: 64px 24px 96px;
    }

    .home-hero {
      background: var(--card-bg);
      border-radius: 20px;
      border: 1px solid var(--border);
      padding: 48px;
      box-shadow: 0 28px 60px rgba(15, 23, 42, 0.12);
    }

    .home-hero h1 {
      font-size: 2.5rem;
      margin-bottom: 16px;
      color: var(--primary);
      letter-spacing: -0.02em;
    }

    .home-hero p {
      color: var(--text-secondary);
      margin-bottom: 32px;
      max-width: 620px;
      font-size: 1.05rem;
      line-height: 1.6;
    }

    .cta-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 20px;
    }

    .cta-card {
      display: flex;
      flex-direction: column;
      gap: 12px;
      background: var(--bg-primary);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 24px;
      text-decoration: none;
      color: inherit;
      box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .cta-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 24px 50px rgba(15, 23, 42, 0.12);
    }

    .cta-card h2 {
      margin: 0;
      font-size: 1.2rem;
      color: var(--primary);
    }

    .cta-card p {
      margin: 0;
      color: var(--text-secondary);
      flex: 1;
      line-height: 1.5;
    }

    .cta-card span {
      font-weight: 600;
      color: var(--color-company);
    }

    @media (max-width: 768px) {
      .home-hero {
        padding: 32px 24px;
      }
    }
  </style>
</ReportLayout>
